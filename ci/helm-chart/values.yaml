# Default values for helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: vanyarock01/tarantool-operator
  tag: 0.1.2-dev
  pullPolicy: Always
